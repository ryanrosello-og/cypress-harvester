# cypress-harvester

TODO :-

[] merged cells
[] pair of repeating elements
[] as chain command
[] ? non-visible rows
[] ? has <thead>
[] ? has <tbody>
[] config:
    - save to file
    - custom column headings